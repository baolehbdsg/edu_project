spring:
  application:
    name: course-service
  cloud:
    nacos:
      config:
        server-addr:  192.168.56.1:8848
        namespace:
        group: course-service
        file-extension: yml  # 默认找服务名.properties文件