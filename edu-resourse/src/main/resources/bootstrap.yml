spring:
  application:
    name: resourse-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace:
        group: resourse-service
        file-extension: yml  # 默认找服务名.properties文件